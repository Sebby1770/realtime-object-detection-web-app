[project]
name = "realtime-object-detection-web-app"
version = "2.0.0"
description = "Dual-runtime realtime object detection: FastAPI + YOLOv8 or in-browser coco-ssd."
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Sebastian Forbes" }]
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
